// Client-safe map constants — imported by MapView (a client component).
// Kept separate from dhabas.ts so that module never enters the browser bundle.

export const DEFAULT_MAP_CENTER = { lat: 37.5, lng: -96 };
export const DEFAULT_MAP_ZOOM = 4;
